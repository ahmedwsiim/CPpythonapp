[Unit]
Description=Gunicorn instance to serve System Monitor App
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/sysmon-app
ExecStart=/home/ubuntu/sysmon-app/venv/bin/gunicorn --workers 3 --bind 0.0.0.0:8080 main:app
Restart=always

[Install]
WantedBy=multi-user.target
